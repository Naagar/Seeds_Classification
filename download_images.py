import panda as pd
import urllib.request


def url_to_jpg(i, urls):
	
	return None


file_name = 'data_file.cvs'

file_path_for_images = 'images/'

urls = pd.read_cvs(file_name)

for i, urls in enumrate(urls.values):

